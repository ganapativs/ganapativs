{
  "name": "ganapativs-profile",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Generates the header image for github.com/ganapativs.",
  "scripts": { "build": "node scripts/generate-assets.mjs" },
  "engines": { "node": ">=18" }
}
